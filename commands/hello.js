exports.run = (client, message, args) => {
    
    message.channel.send(`${message.author} You must be really bored if you are talking to a bot smh. It's ${message.createdAt} go do something useful.`)

}
